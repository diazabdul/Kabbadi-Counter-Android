package com.example.kabaddikounter.ui.theme

import androidx.compose.ui.graphics.Color

// Design token palette
val Jet = Color(0xFF0B0B0D)       // deepest background (dark mode bg)
val Char = Color(0xFF161618)      // card / surface (dark mode)
val Ink = Color(0xFF1F1F23)       // elevated surface / divider
val Rule = Color(0xFF2A2A2F)      // outline / separator
val Mute = Color(0xFF6E6E78)      // muted / secondary text
val Bone = Color(0xFFDCD6C5)      // subtle surface (light mode variant)
val Paper = Color(0xFFECE7DA)     // card surface (light mode)
val Cream = Color(0xFFF5F1E8)     // light mode background / dark mode text

// Accent colors
val Raid = Color(0xFFC6F24E)      // primary accent — lime green
val Raid2 = Color(0xFF9FD423)     // pressed / darker lime

// Semantic colors
val Pos = Color(0xFF4ED39A)       // positive / success
val Neg = Color(0xFFE04A3A)       // negative / error / destructive
val Bonus = Color(0xFFF4B942)     // bonus / warning
val Info = Color(0xFF6DA8F6)      // info / link
